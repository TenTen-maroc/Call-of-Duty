#nullable enable

namespace CoD.Core
{
    /// <summary>Append only: stored as a raw integer in the versioned save.</summary>
    public enum SubtitleSize
    {
        Small,
        Medium,
        Large,
    }
}
