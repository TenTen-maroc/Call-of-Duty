#nullable enable

namespace CoD.Core
{
    /// <summary>Append-only violence presentation setting saved as its numeric value.</summary>
    public enum GoreLevel
    {
        Off = 0,
        Reduced = 1,
        Extreme = 2,
    }
}
